mutation(
  $id: ID!
  $name: String!
  $email: String
  $password: String
) {
  updateUser(
    id: $id
    name: $name
    email: $email
    password: $password
  ) {
    id
    name
    email
  }
}
